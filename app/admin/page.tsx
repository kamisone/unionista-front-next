import React from 'react';
import StoreProvider from '../StoreProvider';

const AdminPage = () => {
    return (
        <StoreProvider>
            <div>Admin</div>
            
        </StoreProvider>
    );
};

export default AdminPage;
