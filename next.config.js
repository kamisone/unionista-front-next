/** @type {import('next').NextConfig} */
const nextConfig = {
    // i18n: {
    //     // localeDetection: true,
    //     locales: ['en', 'fr', 'es'],
    //     defaultLocale: 'fr',
    // },
};

module.exports = nextConfig;
